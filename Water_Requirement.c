#include<stdio.h>
int main()
{
    int n,a;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        scanf("%d",&a);
        int result;
        result=a*2;
        printf("%d\n",result);

    }
    return 0;
}